server:
  host: 0.0.0.0
  port: 8080
  mode: release

database:
  host: localhost
  port: 5432
  user: netmon
  password: netmon_dev_password
  database: netmon
  sslmode: disable
  maxconns: 25
  minconns: 5

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

logging:
  level: info
  format: json
